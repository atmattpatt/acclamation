<!DOCTYPE html >
<html>
  <head>
    <title>Start Acclamation Session</title>
    <meta name="viewport" content="width=device-width, initial-scale=1"/>
    <link rel="stylesheet" type="text/css" href="/stylesheets/pure.css"/>
    <link rel="stylesheet" type="text/css" href="/stylesheets/acclamation.css"/>
  </head>
  <body class="message">
    <div class="pure-g">
      <div class="pure-u-md-1-5"></div>
      <div id="prompt" class="pure-u-1 pure-u-md-3-5 message-box">
        <h1>Welcome to Acclamation</h1>
        <p>
          Acclamation makes retrospectives a breeze.  To get started, you can
          either join an existing session, or create a new session.
        </p>
        <ul>
          <% for(var i = 0; i < sessions.length; i++) { %>
          <li>
            <code><%= sessions[i].id.substr(0, 8) %></code>
            Some Named Session
            <a href="/moderator/<%= sessions[i].id %>">Join as Moderator</a>
            <a href="/client/<%= sessions[i].id %>">Join as Participant</a>
          </li>
          <% } %>
        </ul>
        <p>
          <form action="/session" method="POST" class="pure-form">
            <fieldset>
              <legend>Start New Session</legend>
              <input type="text" id="name" placeholder="Session Name" />
              <button type="submit" class="pure-button pure-button-primary">
                Start Session
              </button>
            </fieldset>
          </form>
        </p>
      </div>
    </div>
  </body>
</html>
